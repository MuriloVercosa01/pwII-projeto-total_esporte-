CREATE DATABASE total_esportes;
use total_esportes;


CREATE TABLE cadastros(
    id int AUTO_INCREMENT PRIMARY KEY,
    nome varchar(100),
    email varchar(100),
    senha varchar(100)
);
CREATE TABLE Categoria(
    id_cate int AUTO_INCREMENT PRIMARY KEY,
    categoria_prod varchar(30)
);
CREATE TABLE produto (
    id_produto int AUTO_INCREMENT PRIMARY KEY,
	img varchar(300),
    modelo varchar(30),
    categoria INT,
    desc_breve varchar(150),
    valor double,
	FOREIGN KEY (categoria) REFERENCES Categoria(id_cate)
);

//conteudo a ser inserido no banco de dados abaixo

//categorias a serem adicionadas

INSERT into categoria ( categoria_prod) VALUES ("Society"),("Campo"),("Quadra");

INSERT INTO produto (modelo,categoria,desc_breve,valor) values ("tenis1",2,"tenis de campo",1500.00);
INSERT INTO produto (modelo,categoria,desc_breve,valor) values ("tenis2",1,"tenis de society",500.00);
INSERT INTO produto (modelo,categoria,desc_breve,valor) values ("tenis3",3,"tenis de quadra",700.00);
INSERT INTO produto (modelo,categoria,desc_breve,valor) values ("tenis4",2,"tenis de campo 2",800.00);
//selecionar informações do produto
SELECT
produto.id_produto,
    produto.modelo,
    categoria.categoria_prod,
    produto.desc_breve,
    produto.valor
FROM
	produto
JOIN
	categoria ON produto.categoria = categoria.id_cate;


    